import React, { useState } from 'react';

const Register = () => {
  const [form, formData] = useState([]);

  return (
    <form>
      <label htmlFor=''></label>
    </form>
  );
};

export default Register;
